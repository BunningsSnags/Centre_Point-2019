#ifndef TRACKER_H_
#define TRACKER_H_
#include <Common.h>

class Tracker {
public:
    int createMaze(int rows, int collums);
    int tileMove();
    // placeTile(Tile obj);
private:
    // int start;
};

#endif